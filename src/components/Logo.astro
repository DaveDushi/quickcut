---
interface Props {
  class?: string;
  variant?: "wordmark" | "mark";
}

const { class: className = "h-8 w-auto", variant = "wordmark" } = Astro.props;
const src = variant === "mark" ? "/gray-rocks.png" : "/wellrox-logo.png";
---

<img src={src} alt="Wellrox" class={className} />
